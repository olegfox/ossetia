{% extends "::base.html.twig" %}

{% block stylesheets %}
    <link href="https://fonts.googleapis.com/css?family=PT+Sans:400,400i,700,700i&subset=cyrillic,cyrillic-ext,latin-ext" rel="stylesheet">
    {% stylesheets
    'bundles/sitemain/frontend/css/bootstrap.min.css'
    'bundles/sitemain/frontend/css/theme.css'
    filter='yui_css' %}
        <link href="{{ asset_url }}" type="text/css" rel="stylesheet" />
    {% endstylesheets %}
{% endblock %}

{% block body %}
    <div class="wrap">
        <div class="header container">
            <a href="{{ path('frontend_homepage') }}"><img src="{{ asset('bundles/sitemain/frontend/images/logo.png') }}"></a>
            <a href="{{ path('frontend_homepage') }}" class="brand">Республиканская политическая партия<br>&laquo;ЕДИНАЯ ОСЕТИЯ&raquo;</a>
            <div class="search">
                <form action="{{ path('frontend_search') }}" method="get">
                    <input type="hidden" name="searchid" value="2279008" />
                    <input type="hidden" name="web" value="0" />
                    <input type="text" name="text" placeholder="Поиск по сайту">
                </form>
            </div>
        </div>
        {% block nav %}
        <div class="nav container">
            {{ knp_menu_render('frontend_main_menu', { 'currentClass': 'current', 'style': 'navbar nav-justified' }) }}
        </div>
        {% endblock %}
        {% block content %}
        <div class="container">
            {% block main_content %}
            {% endblock %}
            {% block right_block %}
                {{ render(url('frontend_quote_main')) }}
            {% endblock %}
        </div>
        {% endblock %}
    </div>
    <div class="footer container">
        <div class="footer--inner">
            Республика Южная Осетия, г. Цхинвал, улица Исака Харебова 18, тел.: 89298115511; &copy; 2012-2016, Партия &laquo;ЕДИНАЯ ОСЕТИЯ&raquo;. Все права защищены. При полном или частичном использовании материалов ссылка на ресурс обязательна. Дата создания сайта: 2016 г.
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {% javascripts
    'bundles/sitemain/frontend/js/jquery-1.10.2.min.js'
    filter='yui_js' %}
    <script type="text/javascript" src="{{ asset_url }}" id="scripts"></script>
    {% endjavascripts %}
{% endblock %}