{% if quote.id is defined %}
<div class="right-block">
    <div class="quote-block">
        {{ quote.text|raw }}<br>
        <span class="author">{{ quote.title }}</span>
    </div>
</div>
{% endif %}